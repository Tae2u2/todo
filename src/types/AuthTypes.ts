export interface UserInfoState {
    email : string;
    password : string;
    id : string;
    updatedAt : string;
    createdAt : string;
}
export interface AuthState {
    email: string;
    password: string;
  }